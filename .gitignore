# Tracks. Large binaries with their own licensing. Fetch your own with
# `agent-tunes download <url>`.
audio/

# Runtime state: enabled flag, pidfile, mpv socket, active sessions, log.
state/

# Compiled from src/audio-watch.swift by `agent-tunes build`.
libexec/audio-watch

.DS_Store

# Your local settings. Copy config.example.env and edit.
config.env
